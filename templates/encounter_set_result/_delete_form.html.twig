<form method="post" action="{{ path('app_set_result_delete', {'id': encounter_set_result.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ encounter_set_result.id) }}">
    <button class="btn">Delete</button>
</form>
